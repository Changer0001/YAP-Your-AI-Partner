version: '3.8'

services:
  llm_assistant:
    build:
      context: ./app    # Points to directory containing Dockerfile, requirements.txt, myapp/
      dockerfile: Dockerfile
    container_name: llm_assistant_container
    ports:
      - "8000:8000"
    env_file: ./app/.env   # Adjust path if needed
